name: Build Windows EXEs

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-build.txt

      - name: Set app version
        shell: pwsh
        run: |
          if ("${{ github.ref_type }}" -eq "tag") {
            "JKWORLD_APP_VERSION=${{ github.ref_name }}" >> $env:GITHUB_ENV
          } else {
            "JKWORLD_APP_VERSION=${{ github.sha }}" >> $env:GITHUB_ENV
          }

      - name: Build Player and Studio
        run: python scripts/build_exe.py

      - name: Zip distributions
        shell: pwsh
        run: |
          Compress-Archive -Path dist/JKWorldNoBrain/* -DestinationPath "JKWorld-NoBrain.zip"
          Compress-Archive -Path dist/JKWorldStudio/* -DestinationPath "JKWorld-Studio.zip"

      - uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: JKWorld-Windows
          path: |
            JKWorld-NoBrain.zip
            JKWorld-Studio.zip

      - name: Publish public download release
        if: startsWith(github.ref, 'refs/tags/v')
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.DISTRIBUTION_TOKEN }}
        run: |
          $repo = "crazyko2002/JKWorld-Downloads"
          $tag = "${{ github.ref_name }}"
          gh release view $tag --repo $repo *> $null
          if ($LASTEXITCODE -eq 0) {
            gh release upload $tag "JKWorld-NoBrain.zip" "JKWorld-Studio.zip" --clobber --repo $repo
          } else {
            gh release create $tag "JKWorld-NoBrain.zip" "JKWorld-Studio.zip" --repo $repo --title "JK世界 $tag" --generate-notes --latest
          }
